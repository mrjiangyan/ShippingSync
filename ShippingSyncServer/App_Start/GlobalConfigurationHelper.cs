using System;
using System.Net.Http.Formatting;
using System.Net.Http.Headers;
using Newtonsoft.Json;

namespace ShippingSyncServer
{
    public static class GlobalConfigurationHelper
    {
        //public static JsonMediaTypeFormatter Init()
        //{
        //    var serializerSettings = new JsonSerializerSettings {NullValueHandling = NullValueHandling.Ignore};
        //    var jsonFormatter = new JsonNetFormatter {SerializerSettings = serializerSettings};
        //    jsonFormatter.MediaTypeMappings.Add(new RequestHeaderMapping("accept", "application/json", StringComparison.InvariantCultureIgnoreCase, true, new MediaTypeHeaderValue("application/json")));
        //    return jsonFormatter;
        //}

      
    }

}
